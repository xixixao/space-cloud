// Generated by CoffeeScript 1.6.2
(function() {
  var Answer, CommentA, CommentQ, File, Q, Question, Topic, User, authenticated, canRead, canWrite, passport, wipe, _ref, _ref1;

  Q = require('q');

  (require('./q-each'))(Q);

  passport = require("passport");

  _ref = require('./model'), wipe = _ref.wipe, Topic = _ref.Topic, User = _ref.User, File = _ref.File, CommentA = _ref.CommentA, CommentQ = _ref.CommentQ, Question = _ref.Question, Answer = _ref.Answer;

  _ref1 = require('./authentication'), canRead = _ref1.canRead, canWrite = _ref1.canWrite, authenticated = _ref1.authenticated;

  module.exports = function(app) {
    var usersFiles;

    app.get('/users/:login', function(request, response) {
      return usersFiles(request.params.login).then(function(user) {
        return response.send(user);
      }, function(error) {
        return response.send(error);
      });
    });
    usersFiles = function(userId) {
      return Q.ninvoke(User, 'findById', userId).then(function(user) {
        return Q.ninvoke(user, 'populate', 'topics.code');
      }).then(function(user) {
        var fileLists, topicPermissions;

        user = user.toObject();
        topicPermissions = user.topics;
        fileLists = Q.map(topicPermissions, function(_arg) {
          var code, topicId;

          code = _arg.code;
          topicId = code._id;
          return Q.ninvoke(File, 'find', {
            topicCode: topicId
          });
        });
        return fileLists.thenEach(function(files, i) {
          return user.topics[i].code.files = files;
        }).then(function() {
          return user;
        });
      });
    };
    app.post('/users', function(request, response) {
      var user;

      user = new User({
        name: request.body.name,
        _id: request.body._id,
        password: request.body.password,
        topics: request.body.topics
      });
      return user.save(function(err) {
        if (err != null) {
          return response.send(err);
        } else {
          return response.send(user);
        }
      });
    });
    app.post('/login', passport.authenticate('local'), function(request, response) {
      return response.send("logged in");
    });
    app.post('/topics', function(request, response) {
      var topic;

      topic = new Topic({
        name: request.body.name,
        _id: request.body._id
      });
      return topic.save(function(err) {
        if (err != null) {
          return response.send(err);
        } else {
          return response.send(topic);
        }
      });
    });
    app.get('/topics/:code', function(request, response) {
      var topicCode;

      topicCode = request.params.code;
      return Topic.findById(topicCode, function(err, docs) {
        if (err != null) {
          return response.send("not found");
        } else {
          return response.send(docs);
        }
      });
    });
    app.post('/users/:login', function(request, response) {
      return User.findById(request.params.login, function(err, user) {
        var topic, topics, _i, _len;

        if (err != null) {
          return response.send(err);
        } else {
          topics = request.body.topics;
          for (_i = 0, _len = topics.length; _i < _len; _i++) {
            topic = topics[_i];
            user.topics.addToSet(topic);
          }
          return response.send(user);
        }
      });
    });
    app.post('/files', authenticated, function(request, response) {
      var file;

      if (!canWrite(request, request.body.topicCode)) {
        return response.send(401, 'User doesnt have write permission');
      }
      file = new File({
        _id: request.body._id,
        path: request.body.path,
        name: request.body.name,
        owner: request.body.owner,
        topicCode: request.body.topicCode
      });
      return file.save(function(err) {
        if (err != null) {
          return response.send(err);
        } else {
          return response.send(file);
        }
      });
    });
    app.get('/files/:code', authenticated, function(request, response) {
      return File.findById(request.params.code, function(err, file) {
        if ((err != null) || (file == null)) {
          return response.send(404, "not found");
        } else if (!canRead(request, file.topicCode)) {
          return response.send(401, "cant read");
        } else {
          return response.send(file);
        }
      });
    });
    app.post('/questions', authenticated, function(request, response) {
      var filePromise;

      filePromise = Q.ninvoke(File, 'findById', request.body.file);
      return filePromise.then(function(file) {
        var question;

        if (file == null) {
          return response.send(404, "File not found");
        }
        if (!canRead(request, file.topicCode)) {
          return response.send(401, 'User doesnt have write permission');
        }
        question = new Question({
          _id: request.body._id,
          owner: request.body.owner,
          filePosition: request.body.filePosition,
          file: request.body.file,
          text: request.body.text
        });
        return question.save(function(err) {
          if (err != null) {
            return response.send(err);
          } else {
            return response.send(question);
          }
        });
      }, function(error) {
        return response.send(500, "File could not be found");
      }).done();
    });
    app.get('/questions/:code', authenticated, function(request, response) {
      return Question.findById(request.params.code, function(err, question) {
        var filePromise;

        if ((err != null) || !question) {
          return response.send(404, "question not found");
        } else {
          filePromise = Q.ninvoke(File, 'findById', question.file);
          return filePromise.then(function(file) {
            if (file == null) {
              return response.send(404, "File not found");
            }
            if (!canRead(request, file.topicCode)) {
              return response.send(401, 'User doesnt have write permission');
            } else {
              return response.send(question);
            }
          }, function(error) {
            return response.send(500, "File could not be found");
          }).done();
        }
      });
    });
    app.post('/answers', authenticated, function(request, response) {
      var questionPromise;

      questionPromise = Q.ninvoke(Question, 'findById', request.body.question);
      return questionPromise.then(function(question) {
        var filePromise;

        if (question == null) {
          return response.send(404, "Question not found");
        }
        filePromise = Q.ninvoke(File, 'findById', question.file);
        return filePromise.then(function(file) {
          var answer;

          if (file == null) {
            return response.send(404, "File not found");
          }
          if (!canRead(request, file.topicCode)) {
            return response.send(401, 'User doesnt have write permission');
          }
          answer = new Answer({
            _id: request.body._id,
            owner: request.body.owner,
            question: request.body.question,
            rank: request.body.rank,
            text: request.body.text
          });
          return answer.save(function(err) {
            if (err != null) {
              return response.send(err);
            } else {
              return response.send(question);
            }
          });
        }, function(error) {
          return response.send(500, "File could not be found");
        }).done();
      }, function(error) {
        return response.send(500, "File could not be found");
      }).done();
    });
    app.get('/answers/:code', authenticated, function(request, response) {
      return Q.ninvoke(Answer, 'findById', request.params.code).fail(function(error) {
        return response.send(500, "Answer could not be found");
      }).then(function(answer) {
        if ((typeof err !== "undefined" && err !== null) || !answer) {
          return response.send(404, "answer not found");
        } else {
          return Q.ninvoke(Question, 'findById', answer.question).fail(function(error) {
            return response.send(500, "Question could not be found");
          }).then(function(question) {
            if (question == null) {
              response.send(404, "Question not found");
              throw "";
            }
            return Q.ninvoke(File, 'findById', question.file).fail(function(error) {
              return response.send(500, "File could not be found");
            }).then(function(file) {
              if (file == null) {
                return response.send(404, "File not found");
              }
              if (!canRead(request, file.topicCode)) {
                return response.send(401, 'User doesnt have write permission');
              } else {
                question.answers.addToSet(answer);
                response.send(question);
                return question.save();
              }
            });
          });
        }
      }).done();
    });
    app.post('/commentsQ', authenticated, function(request, response) {
      var comment;

      comment = new CommentQ({
        _id: request.body._id,
        owner: request.body.owner,
        question: request.body.question,
        text: request.body.text
      });
      return comment.save(function(err) {
        if (err != null) {
          return response.send(err);
        } else {
          Q.ninvoke(Question, 'findById', request.body.question).then(function(question) {
            question.comments.addToSet(comment);
            return question.save();
          });
          return response.send(comment);
        }
      });
    });
    app.get('/commentsQ/:code', authenticated, function(request, response) {
      return CommentQ.find({
        _id: request.params.code
      }, function(err, docs) {
        if (err != null) {
          return response.send("not found");
        } else {
          return response.send(docs);
        }
      });
    });
    app.post('/commentsA', authenticated, function(request, response) {
      var comment;

      comment = new CommentA({
        _id: request.body._id,
        owner: request.body.owner,
        answer: request.body.answer,
        text: request.body.text
      });
      return comment.save(function(err) {
        if (err != null) {
          return response.send(err);
        } else {
          Q.ninvoke(Answer, 'findById', request.body.answer).then(function(answer) {
            answer.comments.addToSet(comment);
            return answer.save();
          });
          return response.send(comment);
        }
      });
    });
    app.get('/commentsA/:code', authenticated, function(request, response) {
      return CommentA.find({
        _id: request.params.code
      }, function(err, docs) {
        if (err != null) {
          return response.send("not found");
        } else {
          return response.send(docs);
        }
      });
    });
    return app.get('/feeds/:user', authenticated, function(request, response) {
      return Q.ninvoke(User.findById(request.params.user).select('topics.code'), 'exec').then(function(_arg) {
        var code, topicCodes, topics;

        topics = _arg.topics;
        topicCodes = (function() {
          var _i, _len, _results;

          _results = [];
          for (_i = 0, _len = topics.length; _i < _len; _i++) {
            code = topics[_i].code;
            _results.push(code);
          }
          return _results;
        })();
        return Q.ninvoke(Question.find({}).populate({
          path: 'file',
          match: {
            topicCode: {
              $in: topicCodes
            }
          }
        }).sort('-timestamp'), 'exec').then(function(questions) {
          var q, _i, _len, _results;

          _results = [];
          for (_i = 0, _len = questions.length; _i < _len; _i++) {
            q = questions[_i];
            if (q.file != null) {
              _results.push(q);
            }
          }
          return _results;
        });
      }).then(function(questions) {
        return response.send(questions);
      }).done();
    });
  };

}).call(this);
